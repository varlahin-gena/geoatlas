# Placeholder so @include "/etc/syslog-ng/conf.d/*.conf" is never an empty glob.
# Buffer overrides: zz_profile.conf (start.sh / tune-resources.sh).
# Ingest token: zz_ingest_auth.conf (start.sh) overrides ga_ingest_token.
@define ga_keep 1
@define ga_ingest_token ""
