# HTTP-only fallback / reference. Runtime default.conf is generated by
# frontend/docker-entrypoint.sh (HTTP or HTTPS + optional redirect).
server {
    listen 80;
    server_name _;
    include /etc/nginx/includes/app.inc;
}
